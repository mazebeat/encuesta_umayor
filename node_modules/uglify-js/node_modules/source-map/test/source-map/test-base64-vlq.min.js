if (typeof define !== "function") {
	var define = require("amdefine")(module, require)
}
define(function (b, a, c) {
	var d = b("../../lib/source-map/base64-vlq");
	a["test normal encoding and decoding"] = function (g, f) {
		var e;
		for (var h = -255; h < 256; h++) {
			e = d.decode(d.encode(h));
			g.ok(e);
			g.equal(e.value, h);
			g.equal(e.rest, "")
		}
	}
});