if (typeof define !== "function") {
	var define = require("amdefine")(module, require)
}
define(function (b, a, e) {
	var g = {};
	var d = {};
	"ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/".split("").forEach(function (i, h) {
		g[i] = h;
		d[h] = i
	});
	a.encode = function c(h) {
		if (h in d) {
			return d[h]
		}
		throw new TypeError("Must be between 0 and 63: " + h)
	};
	a.decode = function f(h) {
		if (h in g) {
			return g[h]
		}
		throw new TypeError("Not a valid base 64 digit: " + h)
	}
});