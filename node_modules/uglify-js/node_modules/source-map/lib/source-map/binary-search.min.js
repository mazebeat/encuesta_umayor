if (typeof define !== "function") {
	var define = require("amdefine")(module, require)
}
define(function (b, a, d) {
	function e(g, i, k, l, f) {
		var h = Math.floor((i - g) / 2) + g;
		var j = f(k, l[h], true);
		if (j === 0) {
			return l[h]
		} else {
			if (j > 0) {
				if (i - h > 1) {
					return e(h, i, k, l, f)
				}
				return l[h]
			} else {
				if (h - g > 1) {
					return e(g, h, k, l, f)
				}
				return g < 0 ? null : l[g]
			}
		}
	}

	a.search = function c(g, h, f) {
		return h.length > 0 ? e(-1, h.length, g, h, f) : null
	}
});